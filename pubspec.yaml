name: music_list_page_lego
description: "beautiful music list page by june"
#publish_to: 'none'
repository: https://github.com/flutter-lego/music_list_page_lego

version: 1.0.1

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  june_flow_util: #@add
  styled_widget: #@add
  modal_bottom_sheet: #@add
  gap: #@add
  flutter_svg: #@add
  flutter_neumorphic_plus: #@add

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

flutter:
  assets:
    - assets/lego/music_list_page_lego/
  uses-material-design: true

topics:
  - lego